```cpp
class Solution {
   public:
    int getMissingNumber(vector<int> &nums) {
        int res = nums.size() * (nums.size() + 1) / 2;
        for (auto x : nums)
            res -= x;
        return res;
    }
};
```
