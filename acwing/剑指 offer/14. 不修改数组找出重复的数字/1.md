<!--
 * @Author: tkzzzzzz6
 * @Date: 2026-04-14 08:26:38
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-14 08:56:37
-->
题目:
给定一个长度为 $n + 1$ 的数组nums，数组中所有的数均在 $1 \sim n$ 的范围内，其中 $n \ge 1$。

请找出数组中任意一个重复的数，但不能修改输入的数组。

数据范围
$1 \le n \le 1000$

样例
给定 nums = [2, 3, 5, 4, 3, 2, 6, 7]。

返回 2 或 3。
思考题：如果只能使用 $O(1)$ 的额外空间，该怎么做呢？

方法 1:数组哈希

```cpp
/*
 * @Author: tkzzzzzz6
 * @Date: 2026-04-14 08:24:21
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-14 08:24:26
 */
/*
* @acwing app=acwing.cn id=15 lang=C++
*
* 14. 不修改数组找出重复的数字
*/

// @acwing code start

class Solution {
public:
    int duplicateInArray(vector<int>& nums) {
        int n = nums.size()-1;
        for(auto x:nums){
            if(x > n || x < 1)return -1;
        }
        vector<int> hash(nums.size(),0);
        for(auto x:nums){
            if(hash[x] != 0){
                return x;
            }
            hash[x] += 1;
        }
        return -1;
    }
};
// @acwing code end

```

方法 2:抽屉原理+二分

```cpp
/*
 * @Author: tkzzzzzz6
 * @Date: 2026-04-14 08:24:21
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-14 08:46:06
 */
/*
* @acwing app=acwing.cn id=15 lang=C++
*
* 14. 不修改数组找出重复的数字
*/

// @acwing code start

class Solution {
public:
    int duplicateInArray(vector<int>& nums) {
        int l = 1,r = nums.size()-1;
        while(l < r){
            int mid = (l + r) >> 1;
            int cnt = 0;
            for(auto x:nums)cnt+=x >= l && x <= mid;
            if(cnt > mid - l + 1)r = mid; //多的数在左边
            else l = mid + 1;
        }
        return l;
    }
};
// @acwing code end

```

