class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        num.sort()
        n = len(nums)
        # 三元组的顺序不重要
        for i in range(n-2)
            x = num[i]
            if i > 0 and x = num[i-1]:
                continue
            j = i + 1
            k = n - 1
            while j < k:
                