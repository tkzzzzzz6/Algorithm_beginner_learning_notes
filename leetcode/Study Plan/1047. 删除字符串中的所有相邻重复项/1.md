<!--
 * @Author: tkzzzzzz6
 * @Date: 2026-04-16 00:14:27
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-16 00:19:01
-->

思路:使用栈的思想,

方法 1:单独声明和使用一个栈,遍历字符串,如果当前字符和栈顶元素相同,说明有相邻重复项,弹出栈顶元素;否则将当前字符压入栈中。最后将栈中的元素依次弹出并反转得到结果字符串。

```cpp
/*
 * @lc app=leetcode.cn id=1047 lang=cpp
 * @lcpr version=30204
 *
 * [1047] 删除字符串中的所有相邻重复项
 */

using namespace std;
#include <algorithm>
#include <array>
#include <bitset>
#include <climits>
#include <deque>
#include <functional>
#include <iostream>
#include <list>
#include <queue>
#include <stack>
#include <tuple>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// @lcpr-template-end
// @lc code=start
class Solution {
public:
    string removeDuplicates(string s) {
        stack<char> st;
        for(auto c:s){
            if(st.empty() || c != st.top())st.push(c);
            else st.pop();
        }

        string res = "";
        while(!st.empty()){
            res += st.top();
            st.pop();
        }

        reverse(res.begin(),res.end());
        return res;
    }
};
```

方法 2:直接在字符串的基础上进行操作

```cpp
/*
 * @Author: tkzzzzzz6
 * @Date: 2026-04-16 00:13:48
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-16 00:16:52
 */
/*
 * @lc app=leetcode.cn id=1047 lang=cpp
 * @lcpr version=30204
 *
 * [1047] 删除字符串中的所有相邻重复项
 */

using namespace std;
#include <algorithm>
#include <array>
#include <bitset>
#include <climits>
#include <deque>
#include <functional>
#include <iostream>
#include <list>
#include <queue>
#include <stack>
#include <tuple>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>
// @lcpr-template-end
// @lc code=start
class Solution {
public:
    string removeDuplicates(string s) {
        string res;
        for(auto c:s){
            if(res.empty() || res.back() != c){
                res.push_back(c);
            }else{
                res.pop_back();
            }
        }
        return res;
    }
};
```
