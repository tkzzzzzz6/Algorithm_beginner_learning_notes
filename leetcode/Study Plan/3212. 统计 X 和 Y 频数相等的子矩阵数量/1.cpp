#include <vector>

using namespace std;
class Solution {
public:
    int numberOfSubmatrices(vector<vector<char>>& grid) {
        int m = grid.size(),n = grid[0].size();
        vector<vector<int>> x_cnt(m,vector<int>(n));
        vector<vector<int>> y_cnt(m,vector<int>(n));
        
        
    }
};
