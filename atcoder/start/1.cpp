#include <bits/stdc++.h>

using namespace std;

int main(){
    int a,b;
    cin >> a >> b;
    a += b;
    cin >> b;
    a += b;
    string s;
    cin >> s;
    cout << a << " " << s;

    return 0;
}