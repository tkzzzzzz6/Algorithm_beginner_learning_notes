{
	"template": {
	  "prefix": "ac",
	  "body": [
		"import sys, math",
		"from collections import deque, defaultdict, Counter",
		"import heapq, bisect",
		"input = lambda: sys.stdin.readline().rstrip()",
		"",
		"def solve():",
		"    $0",
		"",
		"if __name__ == \"__main__\":",
		"    sys.setrecursionlimit(10**7)",
		"    t = 1",
		"    # t = int(input())",
		"    for _ in range(t):",
		"        solve()"
	  ],
	  "description": "Python 通用算法模板（快读 + solve 结构）"
	}
  }
  