<!--
 * @Author: tkzzzzzz6
 * @Date: 2026-04-12 13:22:20
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-12 13:45:39
-->
zhe
这道题需要注意的是,结果不能是重复的三元组,需要对 3 个数进行去重,但是三元组中的 3 个元素可以相同


方法 1:哈希表

这里需要注意的是去重的问题,使用哈希的话会比较复杂



方法 2:排序+双指针

先排序,然后固定一个数,使用双指针在剩下的数中寻找满足条件的组合,需要注意去重的操作
> 注意:去重的操作需要在 left 和 right 的去重需要在 result 存储答案之后运行,不能直接在整体 while 循环中去重,因为如果是全 0 的情况,如果在 while 循环中去重,会导致 left 和 right 都停在同一个位置,没有答案输出

```cpp

需要注意去重的操作

```cpp
/*
 * @Author: tkzzzzzz6
 * @Date: 2026-04-12 13:35:46
 * @LastEditors: tkzzzzzz6
 * @LastEditTime: 2026-04-12 13:43:26
 */
class Solution {
public:
  vector<vector<int>> threeSum(vector<int> &nums) {
    vector<vector<int>> result;
    int n = nums.size();
    sort(nums.begin(), nums.end()); // 排序

    for (int i = 0; i < n; ++i) {
      if (i > 0 && nums[i] == nums[i - 1])
        continue;
      int left = i + 1, right = n - 1;
      while (left < right) {
        long long s = nums[i] + nums[left] + nums[right];
        if (s > 0)
          right--;
        else if (s < 0)
          left++;
        else {
          result.push_back({nums[i], nums[left], nums[right]}); // 存储数值而不是索引
          while (left < right && nums[left] == nums[left + 1]) { // 添加边界检查
            left++;
          }
          while (left < right &&
                 nums[right] == nums[right - 1]) { // 添加边界检查
            right--; // 应该是 right--，不是 right++
          }
          left++;  // 继续向右移动
          right--; // 继续向左移动
        }
      }
    }
    return result;
  }
};

```
