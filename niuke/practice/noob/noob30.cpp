
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios::sync_with_stdio(false), cin.tie(0);

    int a, b;
    do{
        cin >> a >> b;
        if (a == 0 && b == 0)break;
        cout << a + b << '\n';
        
    } while (1);

    return 0;
}